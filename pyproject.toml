[project]
name = "apple-edge-llm-lab"
version = "0.1.0"
description = "Benchmarks + patches for LLM inference on Apple Silicon (MPS/MLX/Metal)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "torch",
  "transformers",
  "safetensors",
  "mlx-lm; sys_platform == 'darwin'",
]